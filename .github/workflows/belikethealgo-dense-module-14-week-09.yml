name: BeLikeTheAlgo - dense evidence module 14 week 9

on:
  push:
    branches: [belikethealgo-course-study]
    paths: ['.github/workflows/belikethealgo-dense-module-14-week-09.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dense-week-09:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: belikethealgo-course-study
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install MEGA reader and visual dependencies
        run: |
          cd projects/belikethealgo-course-study
          npm init -y >/dev/null 2>&1
          npm install --no-save megajs@1.3.10
          python -m pip install --upgrade pip
          python -m pip install opencv-python-headless pillow
      - name: Download published Week 9 source video only
        env:
          MEGA_PUBLIC_LINK: 'https://mega.nz/folder/QvEXiZpB#dFIeSfaKPvzSLnOEmzleTg'
          MODULES: '14'
          VIDEO_MIN: '9'
          VIDEO_MAX: '9'
        run: node projects/belikethealgo-course-study/scripts/mega_download_modules.mjs
      - name: Verify exactly one Week 9 MP4 was downloaded
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          data = json.loads(Path('projects/belikethealgo-course-study/work/raw/_download_summary.json').read_text())
          if data.get('count') != 1:
              raise SystemExit(f"Expected exactly one Week 9 MP4, got {data.get('count')}")
          print(data)
          PY
      - name: Extract dense visual evidence
        run: |
          set -euo pipefail
          ROOT='projects/belikethealgo-course-study'
          VIDEO="$(find "$ROOT/work/raw" -type f -iname '*.mp4' | head -1)"
          test -n "$VIDEO"
          OUT="$ROOT/work/dense/week-09"
          mkdir -p "$OUT"
          # Full chart-review body at 1 fps. Week 9 is ~35m34s.
          python "$ROOT/scripts/dense_extract.py" "$VIDEO" "$OUT/baseline-1fps" \
            --fps 1 --interval 150:750 --interval 750:1350 --interval 1350:1950 --interval 1950:2125
          # 2 fps focus pass across decision, drawing and management regions. Broad coverage is intentional for the first v2 pass.
          python "$ROOT/scripts/dense_extract.py" "$VIDEO" "$OUT/focus-2fps" \
            --fps 2 --interval 220:900 --interval 930:1530 --interval 1560:2125
          rm -rf "$OUT/baseline-1fps/frames" "$OUT/focus-2fps/frames"
      - name: Write evidence manifest
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          root=Path('projects/belikethealgo-course-study/work/dense/week-09')
          manifest={'week':9,'protocol':'ANALYSIS_PROTOCOL.md + measurement_architecture_v2 + weekly_trade_event_schema_v2','baseline':json.loads((root/'baseline-1fps'/'dense_index.json').read_text()),'focus':json.loads((root/'focus-2fps'/'dense_index.json').read_text()),'note':'Dense frames consolidated into timestamped contact sheets after extraction.'}
          (root/'manifest.json').write_text(json.dumps(manifest,ensure_ascii=False,indent=2))
          print('baseline frames:',manifest['baseline']['frame_count']); print('focus frames:',manifest['focus']['frame_count'])
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: belikethealgo-module-14-week-009-dense-evidence-v2
          path: projects/belikethealgo-course-study/work/dense/week-09
          if-no-files-found: error
          retention-days: 2
